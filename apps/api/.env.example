# Server
PORT=3000
NODE_ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=demo_api
DB_USER=postgres
DB_PASSWORD=your_password_here

# Optional: full connection string (overrides above if set)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/demo_api
