import Card from "@/components/Card";
import PageTemplate from "@/components/PageTemplate";

export default function LoginPage() {
  return (
    <Card>
      <PageTemplate>Login dashboard page</PageTemplate>
    </Card>
  );
}
