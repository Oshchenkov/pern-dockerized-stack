import Card from "@/components/Card";
import PageTemplate from "@/components/PageTemplate";

export default function RevenuePage() {
  return (
    <Card>
      <PageTemplate>Revenue page default</PageTemplate>
    </Card>
  );
}
